module("boogie") {
    java {
        basedir "src/"
        include "main/**/*.java"
        include "generated/**/*.java"
    }

    jastadd {
        include "spec/grammar/*.ast"
        include "spec/*.jadd"
        include "spec/*.jrag"
    }

    scanner {
        include "spec/scanner/*.flex"
    }

    parser {
        include "spec/parser/*.parser"
    }
}
