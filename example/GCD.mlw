module Program
  use int.Int
  use int.Abs

  function gcd_recursive (l0: int) (l1: int) : int
  =
    if l0 = l1 then l0
    else if l0 > l1 then (gcd_recursive (l0 - l1) l1)
    else (gcd_recursive l0 (l1 - l0))

  val function a (l1: int) : int
    ensures { result = gcd_recursive 3 4 }

end
